@model IEnumerable<Laundry.Models.Bos>

@{
    ViewData["Title"] = "Bos";
}

<h2>Data Bos</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("ID Bos")
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th>@Html.DisplayName("Manage Datas")</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                <a asp-action="Karyawans">Atur Data Karyawan</a> | <a asp-action="DetailPelayanans">Lihat Detail Pelayanan</a> | <a asp-action="Pesanans">Atur Data Pesanan</a>
            </td>
        </tr>
}
    </tbody>
</table>
